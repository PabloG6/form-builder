import { HfxInputDirective } from './hfx-input.directive';

describe('HfxInputDirective', () => {
  it('should create an instance', () => {
    const directive = new HfxInputDirective();
    expect(directive).toBeTruthy();
  });
});
