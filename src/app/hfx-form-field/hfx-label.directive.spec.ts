import { HfxLabelDirective } from './hfx-label.directive';

describe('HfxLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new HfxLabelDirective();
    expect(directive).toBeTruthy();
  });
});
